{
  "summary": "Full backend smoke test of Grameen Udyog AI Advisory. All 18 checks passed including auth (JWT), calculator scheme routing/EMI/quarterly schedule, boundary case (₹15k margin → Term Loan since project_cost > ₹1.4L), micro cap (₹14k → approved_loan capped at ₹1.25L, capped_by_max=true), Term Loan quarterly schedule length=26, LLM feasibility EN and HI (Devanagari verified), authenticated auto-save + list/get/delete reports, locations dict, business categories list. LLM (Gemini 3.1 Pro) returned valid strict JSON on both calls.",
  "tested_scope": ["backend", "auth", "integration"],
  "passed": [
    "GET /api/",
    "POST /api/auth/register (new + duplicate 400)",
    "POST /api/auth/login (correct + wrong 401)",
    "GET /api/auth/me (with/without token)",
    "POST /api/calculator/compute margin=14000 Micro capped at 125000",
    "POST /api/calculator/compute margin=100000 Term Loan with 26 quarters",
    "POST /api/calculator/compute boundary margin=15000 routes to Term Loan",
    "POST /api/calculator/compute margin=0 returns 400",
    "POST /api/feasibility/generate en (full schema, Gemini 3.1 Pro)",
    "POST /api/feasibility/generate hi (Devanagari in executive_summary)",
    "GET /api/reports lists auto-saved report",
    "GET /api/reports/{id} returns saved report",
    "DELETE /api/reports/{id} removes it (404 after)",
    "GET /api/locations nested dict verified (Maharashtra→Nashik→Sinnar→Musalgaon)",
    "GET /api/business-categories non-empty list"
  ],
  "failed": [],
  "action_items": [],
  "success_rate": {"backend": "100%", "frontend": "N/A (skipped per request)"},
  "retest_needed": false,
  "full_testing_deferred": true
}
